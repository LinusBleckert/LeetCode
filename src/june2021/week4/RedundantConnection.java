package june2021.week4;

public class RedundantConnection {


    // Brute force would be, Check if edges is a tree, if not remove edge and try again
    // until tree is found

    // First edge creates two nodes, the rest adds only one node
    // The amount of edges and amount of nodes are the same, is this significant information?

    // Node can only have two children
    // Check that one node doesnt appear in several places? smtg like that

    // Could do something like trying to balance the tree, if a swap happens its invalid
    // This is
    class Node{

        Node leftChild;
        Node RightChild;


    }

    public int[] FindRedundantConnection(int[][] edges){


        for(int i = 0; i < edges.length; i++){


        }
    }


}
